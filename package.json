{
  "name": "ponto-progredir",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "node scripts/expand-employee-shell.mjs && prisma generate && prisma db push --skip-generate && next build",
    "db:push": "prisma db push",
    "start": "next start",
    "prisma:studio": "prisma studio",
    "seed": "node scripts/seed.js",
    "test": "vitest run"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.1",
    "@types/node": "^26.2.0",
    "@types/react": "19.2.18",
    "prisma": "^4.16.2",
    "typescript": "5.8.2",
    "vitest": "^4.1.11"
  }
}
